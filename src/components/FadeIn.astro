---
const { direction = "up", speed = "1000", delay = "0", class: className = "" } = Astro.props;
---

<div class={`fade-in fade-in-start ${className}`} data-direction={direction} data-speed={speed} data-delay={delay}>
  <slot />
</div>
